/*
Approach - 2
do it just lik we did in convert tree to DLL. in case you want to convert to a singly linked
list then just make prev->left = NULL for every root.

Time Complexity: O(N)
Space Complexity: O(height) */